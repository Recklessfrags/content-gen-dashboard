- **Mobile roster is unreachable**; area: responsive/navigation; evidence: `src/app/globals.css:250-252` hides `.roster` under 880px, while character selection and `+ New character` live only inside that sidebar at `src/components/ControlRoom.tsx:1719-1747`. On 412px/320px the active character can be edited, but switching characters is impossible. Proposed change: keep a compact mobile roster control above the dossier, e.g. a `select` or horizontal character strip rendered in the roster view when `.roster` is hidden; reuse `chars`, `activeId`, `setActiveId`, and `addChar`. IMPACT: High; EFFORT: M.

- **Desktop rail likely overflows at short mobile heights**; area: responsive/navigation; evidence: `.rail` is fixed at `height:100vh` with five 56px nav buttons, vertical brand, signout, gaps, and padding at `src/app/globals.css:25-39`, but has no `overflow-y:auto`. At 320px wide landscape or short browser chrome, lower nav/signout can become inaccessible. Proposed change: add `overflow-y:auto`, reduce rail/button dimensions at small heights via `@media (max-height:620px)`, and preserve visible focus. IMPACT: Medium; EFFORT: S.

- **Read-only metric cards are keyboard stops with no action**; area: accessibility/interaction; evidence: Overview metric cards use `tabIndex={0}` at `src/components/ControlRoom.tsx:387-410`, `440-465`, `493-577`, and Cost cards/audit rows do the same at `src/components/ControlRoom.tsx:727-794`. CSS also gives them hover/active affordance at `src/app/globals.css:699-723`. Proposed change: remove `tabIndex` from static cards and expose the summaries as normal text, or convert only actionable cards into real buttons/links. IMPACT: High; EFFORT: S.

- **Dashboard nav uses `aria-pressed` instead of tab semantics**; area: accessibility/navigation; evidence: the five view controls are rendered as independent buttons with `aria-pressed` at `src/components/ControlRoom.tsx:1677-1695`. This works as toggles, but the UI is a single-view navigation system. Proposed change: use `role="tablist"`/`role="tab"` with `aria-selected`, `aria-controls`, and optional arrow-key navigation, or use semantic links if routes are introduced. IMPACT: Medium; EFFORT: M.

- **Initial load error is a dead end**; area: UX/error recovery; evidence: when any of the initial `characters`, `ideas`, or `episodes` queries fails, the app renders only “Comms down” at `src/components/ControlRoom.tsx:1341-1350` and `1710-1714`, with no retry or sign-out path. Proposed change: add a retry button that reruns the initial load and keep the rail/signout visible, or split loading per domain so one failed read does not blank the whole control room. IMPACT: High; EFFORT: M.

- **Initial `Promise.all` makes optional read failures block core editing**; area: flow/error resilience; evidence: character editing, ideas, and read-only episodes are fetched together at `src/components/ControlRoom.tsx:1341-1349`, and `firstErr` stops the entire app. Runs are pipeline-owned read-only per `DIRECTION.md:13-18` and `src/lib/types.ts:54-55`, so a pipeline table/RLS issue should not prevent editing characters. Proposed change: load roster first, then fetch ideas/runs/cost independently with per-view error states. IMPACT: High; EFFORT: M.

- **Idea logging has no pending state and can double-submit**; area: interaction/race condition; evidence: `logIdea` clears the input immediately and awaits Supabase at `src/components/ControlRoom.tsx:1622-1643`; the “Log it” button remains enabled at `src/components/ControlRoom.tsx:1982-1984`, and Enter also calls `logIdea` at `1979`. Proposed change: add `ideaSaving` or `pendingIdeaText`, disable the input/button while the insert is in flight, and ignore duplicate submissions for the same trimmed text. IMPACT: High; EFFORT: S.

- **Enter key handler can submit unexpectedly when input is later wrapped in a form**; area: interaction robustness; evidence: the Wire input handles Enter via `onKeyDown={(e) => e.key === "Enter" && logIdea()}` at `src/components/ControlRoom.tsx:1975-1980` without `preventDefault()`. Proposed change: either make the row a real `<form onSubmit>` with `preventDefault`, or explicitly prevent default on Enter before calling `logIdea`. IMPACT: Medium; EFFORT: S.

- **Ideas can accidentally lose character assignment**; area: data/UX consistency; evidence: `Idea.character_id` is nullable at `src/lib/types.ts:43-49`, new ideas use `activeId` at `src/components/ControlRoom.tsx:1631`, but the character select value falls back to `""` at `2017-2024` and has no “Unassigned” option. If no characters exist, the select has no matching option. Proposed change: add an explicit “Unassigned” option and persist `null` instead of `""`; in `setIdeaField`, normalize empty values for `character_id`. IMPACT: Medium; EFFORT: S.

- **Wire tag updates have no per-control pending/failed affordance**; area: optimistic-update UX; evidence: `cycleStatus` and `setIdeaField` optimistically update at `src/components/ControlRoom.tsx:1645-1665`, then revert on failure with only a global flash. The controls remain active during the request, so rapid clicks/select changes can race and revert to stale values. Proposed change: track pending idea ids/fields, disable only the affected button/select, and show a small inline error state on the card. IMPACT: Medium; EFFORT: M.

- **Status changes in the dossier are local until save, but the control looks immediate**; area: interaction clarity; evidence: `toggleStatus` only mutates local `chars` at `src/components/ControlRoom.tsx:1616-1619`; the save is separate at `1557-1595`, but the button text at `1904-1906` toggles without dirty-state indication. Proposed change: add a dirty state comparing local active fields to the last saved snapshot, then label the savebar “Unsaved changes” and disable/save only when dirty. IMPACT: High; EFFORT: M.

- **Unsaved character edits can be lost by switching characters/views**; area: flow/data-loss prevention; evidence: field edits are local via `set` at `src/components/ControlRoom.tsx:1407-1410`; selecting another roster card sets `activeId` immediately at `1726-1730`, and nav changes call `setView(k)` at `1686-1690` with no dirty guard. Proposed change: implement dirty tracking and a confirm/discard dialog before changing active character, opening a history preview, or leaving the roster view. IMPACT: High; EFFORT: M.

- **Save error flash may be missed and is not announced**; area: accessibility/error handling; evidence: save/add/idea errors use `showFlash` at `src/components/ControlRoom.tsx:1576-1589`, `1606-1608`, `1637-1640`, but the flash node is only rendered in the roster savebar at `1920-1922` and has no live-region role. Errors triggered from The Wire may be invisible because that savebar is not mounted. Proposed change: move flash/toast to the top-level `.cr`, add `role="status"` for success and `role="alert"` for errors, and keep it mounted across views. IMPACT: High; EFFORT: S.

- **Cost view has no retry path after receipts fail**; area: error recovery; evidence: Cost error state renders the message only at `src/components/ControlRoom.tsx:647-660`; the fetch function is embedded in a mount effect at `1365-1394` and not exposed to `CostBoxDashboard`. Proposed change: extract `fetchCostReceipts` into a `useCallback`, pass `onRetry`, and include a “Retry receipts” button in the Cost error state. IMPACT: Medium; EFFORT: S.

- **Cost and Overview spend can disagree with Run detail spend**; area: information consistency; evidence: Cost computes spend from `max(spend_so_far)` in receipts at `src/components/ControlRoom.tsx:244-288`, Overview uses that value at `323-366`, but Run detail displays `episode.spend` at `902-904` and run cards display `e.spend` at `2102-2104`. Proposed change: standardize labels and values: show “Episode table spend” vs “Receipt live spend”, or compute and pass live spend into run cards/detail when receipts are loaded. IMPACT: Medium; EFFORT: M.

- **Read-only Cost controls look like disabled feature controls**; area: visual/product fit; evidence: the provider card shows a segmented-looking “Providers / Characters Deferred” control at `src/components/ControlRoom.tsx:755-761`, styled as toggle pills at `src/app/globals.css:1063-1087`, but it is static and uses `aria-disabled` on a `span`. Proposed change: render this as a plain disclosure note/badge, not a control; keep it in scope by explaining character attribution requires future pipeline metadata, not a dashboard action. IMPACT: Medium; EFFORT: S.

- **Budget cap placeholder consumes prime visual space without action**; area: visual hierarchy/scope; evidence: `Budget Cap Status` is a parked metric card at `src/components/ControlRoom.tsx:741-752` with a fake empty bar at `src/app/globals.css:1026-1051`. It reads like a broken control. Proposed change: demote to a small disclosure row under the spend total or remove until the pipeline exposes a readable cap API. IMPACT: Medium; EFFORT: S.

- **Wire and Runs share `.wire`/`.wire-list` classes, hiding different semantics**; area: component/style reuse; evidence: both The Wire and Runs render `<div className="wire">` and `.wire-list` at `src/components/ControlRoom.tsx:1962-1994` and `2054-2075`. This causes CSS coupling, e.g. `.wire{position:relative}` added later for run drilldown at `src/app/globals.css:264-265` also affects ideas. Proposed change: extract `ViewShell`, `ViewHeader`, and `ScrollableList` classes/components, then use view-specific modifiers like `.view-shell.runs`. IMPACT: Medium; EFFORT: M.

- **Inline styles create brittle one-off layout/color behavior**; area: style consistency; evidence: mono field styles are inline at `src/components/ControlRoom.tsx:165`, run detail header flex is inline at `883`, retry margin is inline at `918`, Wire row margin is inline at `1974`, and idea status border colors are inline at `1998-2005`. Proposed change: move these to named utility/modifier classes such as `.field-textarea.mono`, `.idea-card.status-active`, and `.detail-title-row`. IMPACT: Low; EFFORT: S.

- **Button and tap target sizes are inconsistent and often small**; area: mobile/accessibility; evidence: nav buttons are 56px at `src/app/globals.css:31-38`, primary `.btn` is padded at `106-107`, but status buttons/selects are `min-height:24px` at `196-200`, history trigger is `min-height:28px` at `77-79`, and close buttons are `min-height:28px` at `319-325`. Proposed change: create size tokens/components and make touch targets at least 40-44px on coarse pointers, while preserving compact desktop density. IMPACT: High; EFFORT: M.

- **Form fields use textareas for single-line data**; area: form UX/accessibility; evidence: the shared `Field` component always renders `<textarea>` at `src/components/ControlRoom.tsx:159-166`, even for Codename `rows={1}` at `1804-1811` and Runtime target `rows={2}` at `1877-1885`. Proposed change: let `Field` render an `<input>` for short scalar values and `<textarea>` for long sections; add `id`/`htmlFor` to improve label association. IMPACT: Medium; EFFORT: M.

- **Labels are not programmatically tied to textareas**; area: accessibility/forms; evidence: `Field` renders a `<label>` sibling followed by a `<textarea>` rather than wrapping the control or using `htmlFor`/`id` at `src/components/ControlRoom.tsx:151-166`. Proposed change: generate a stable id from the field name/active id and use `<label htmlFor={id}>`; pass `name`/`id` through the `Field` props. IMPACT: Medium; EFFORT: S.

- **Dialogs do not restore focus cleanly for nested restore-from-history flow**; area: focus management; evidence: `requestRestore` closes history and opens restore at `src/components/ControlRoom.tsx:1490-1494`; confirm calls `restoreDialogFocus(true)` at `1501-1510`, which falls through to `restoreHistoryFocus` and tries to focus a history trigger, not the save button that must be used next. Proposed change: after confirming restore, focus the highlighted “Save dossier” button and announce the restored-draft warning; keep history-trigger restoration only for cancel/close. IMPACT: Medium; EFFORT: S.

- **Three separate focus-trap implementations are copied**; area: maintainability/accessibility; evidence: near-identical Escape/Tab trapping exists in `DrillDownPanel` at `src/components/ControlRoom.tsx:830-872`, `HistoryDrawer` at `1064-1103`, and `RestoreDialog` at `1218-1257`. Proposed change: extract a small `useFocusTrap({containerRef,onEscape,initialFocusRef,restoreFocus})` hook and use it for all overlays; this reduces drift and makes nested-dialog behavior easier to fix. IMPACT: Medium; EFFORT: M.

- **History preview can overwrite unsaved edits without warning**; area: data-loss prevention; evidence: previewing a revision sets `previewingRevisionId` directly at `src/components/ControlRoom.tsx:1486-1488`; displayed fields switch to `flattenRevision` at `1397-1400`, while any unsaved local edits remain hidden until preview exits. Restore warning exists only after confirm at `1891-1895`. Proposed change: if dirty, show a small confirmation before preview/restore, or explicitly banner “Unsaved live edits are hidden while previewing.” IMPACT: Medium; EFFORT: M.

- **Run detail raw JSON can be expensive and hard to scan**; area: performance/perceived performance; evidence: every receipt render calls `JSON.stringify(receipt.evidence, null, 2)` and `JSON.stringify(receipt.result, null, 2)` inside closed `<details>` at `src/components/ControlRoom.tsx:987-1012`. Large receipts pay serialization cost before the user opens them. Proposed change: move JSON rendering into a lazy `JsonDisclosure` component that stringifies only when opened, and add copy/download actions for long blobs. IMPACT: Medium; EFFORT: M.

- **Long lists have no virtualization or progressive loading**; area: performance/perceived performance; evidence: characters, ideas, episodes, revisions, and audit rows all render full arrays directly at `src/components/ControlRoom.tsx:1725-1744`, `1993-2049`, `2075-2112`, `1163-1200`, and `789-810`. Proposed change: add lightweight search/filter and windowing for the largest lists, or at minimum cap initial render with “show more” for receipts/audit rows. IMPACT: Medium; EFFORT: M.

- **Loading states are spinners that shift whole views**; area: perceived performance; evidence: initial app, cost, history, and run detail loading use centered `.loading` plus `.spin` at `src/components/ControlRoom.tsx:1706-1709`, `639-641`, `1144-1147`, `910-913`; CSS defines only spinner animation at `src/app/globals.css:221-226`. Proposed change: add view-shaped skeleton rows/cards so layout remains stable and users know what is loading. Respect `prefers-reduced-motion` by avoiding shimmer or disabling it. IMPACT: Medium; EFFORT: M.

- **Mobile Wire input row can squeeze the button/input at 320px**; area: responsive/mobile; evidence: `.cap .row` is a fixed horizontal flex row at `src/app/globals.css:185-188`, and the button uses desktop `.btn` padding at `106-107`; no mobile rule stacks this row. Proposed change: under 480px, set `.cap .row{flex-direction:column}` and make the button full-width or icon+label with 44px height. IMPACT: Medium; EFFORT: S.

- **Login errors are not announced and mode switch preserves stale errors**; area: auth UX/accessibility; evidence: login renders `{state.error && <div className="err">...` at `src/app/login/page.tsx:53`, and mode toggling at `54-62` changes sign-in/sign-up without clearing or announcing that error. Proposed change: render errors with `role="alert"`, clear/reset action state on mode change if feasible, and disable the mode toggle while `pending` from `11`/`45-51`. IMPACT: Low; EFFORT: S.

- **Color contrast risk on dim microcopy and status tokens**; area: visual/accessibility; evidence: token values put `--paper-faint:#8F897B` and `--paper-dim:#A39A86` on dark backgrounds at `src/app/globals.css:3-9`, then use them for 9-11px uppercase labels across `.eyebrow`, `.count`, `.chip`, `.rmeta`, `.statusbtn`, and `.tag-select` at `22`, `50`, `60-63`, `196-212`. Proposed change: run contrast checks against `--ink`, `--ink-2`, `--ink-3`; raise dim text luminance or increase small-label font sizes to 12px minimum. IMPACT: Medium; EFFORT: S.

- **Top 5 highest value-to-effort**
- 1. Move flash/toast to a top-level live region so errors from every view are visible and announced.
- 2. Add mobile character switching because the roster is currently hidden below 880px.
- 3. Remove `tabIndex={0}` from static Overview/Cost cards to cut noisy keyboard stops.
- 4. Add pending/disabled state to Wire idea logging to prevent duplicate inserts.
- 5. Add dirty-state warnings for dossier edits before switching character/view or previewing history.
