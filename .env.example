# Supabase — public client config (safe to expose; protected by RLS)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-or-publishable-key

# NOTE: never put the service_role key in this app. RLS is the security boundary.
